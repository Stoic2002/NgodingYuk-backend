package main

import (
	"github.com/arulkarim/ngodingyuk-server/internal/domain"
	"gorm.io/gorm"
)

func seedSQLLessons(db *gorm.DB) {
	lessons := []domain.Lesson{
		// ===== sql-beginner (8 lessons) =====
		{ID: lessonUUID(4, 1), CourseID: CourseSQLBeg, OrderIndex: 1, XPReward: 10,
			TitleID: "Apa itu Database & SQL?", TitleEN: sp("What is a Database & SQL?"),
			ContentMarkdownID: "# Apa itu Database & SQL?\n\n## Database\nDatabase adalah tempat penyimpanan data yang terorganisir. Bayangkan spreadsheet raksasa yang bisa diakses banyak orang sekaligus.\n\n## SQL (Structured Query Language)\nSQL adalah bahasa untuk berinteraksi dengan database relasional.\n\n### Komponen Utama SQL:\n1. **DDL** (Data Definition Language): CREATE, ALTER, DROP\n2. **DML** (Data Manipulation Language): SELECT, INSERT, UPDATE, DELETE\n3. **DCL** (Data Control Language): GRANT, REVOKE\n\n## Database Relasional\nData disimpan dalam tabel (table) yang terdiri dari:\n- **Baris (row)** = satu record data\n- **Kolom (column)** = atribut/field\n\nContoh tabel `mahasiswa`:\n| nim | nama | jurusan | ipk |\n|-----|------|---------|-----|\n| 001 | Budi | Informatika | 3.5 |\n| 002 | Ani  | Sistem Informasi | 3.8 |\n\n## PostgreSQL\nKita akan menggunakan PostgreSQL — database open-source paling populer, dipakai oleh Instagram, Spotify, dan banyak perusahaan besar.",
			ContentMarkdownEN: sp("# What is a Database & SQL?\n\n## Database\nA database is an organized data storage. Think of it as a giant spreadsheet that many people can access simultaneously.\n\n## SQL (Structured Query Language)\nSQL is the language for interacting with relational databases.\n\n### Main SQL Components:\n1. **DDL** (Data Definition Language): CREATE, ALTER, DROP\n2. **DML** (Data Manipulation Language): SELECT, INSERT, UPDATE, DELETE\n3. **DCL** (Data Control Language): GRANT, REVOKE\n\n## Relational Database\nData is stored in tables consisting of:\n- **Rows** = one data record\n- **Columns** = attributes/fields\n\nExample `students` table:\n| id | name | major | gpa |\n|-----|------|---------|-----|\n| 001 | Budi | CS | 3.5 |\n| 002 | Ani  | IS | 3.8 |\n\n## PostgreSQL\nWe'll use PostgreSQL — the most popular open-source database, used by Instagram, Spotify, and many large companies."),
		},
		{ID: lessonUUID(4, 2), CourseID: CourseSQLBeg, OrderIndex: 2, XPReward: 10,
			TitleID: "SELECT Dasar", TitleEN: sp("Basic SELECT"),
			ContentMarkdownID: "# SELECT Dasar\n\nSELECT digunakan untuk mengambil data dari tabel.\n\n## Mengambil Semua Kolom\n```sql\nSELECT * FROM produk;\n```\n\n## Memilih Kolom Tertentu\n```sql\nSELECT nama_produk, harga FROM produk;\n```\n\n## Alias (AS)\n```sql\nSELECT nama_produk AS nama, harga AS price FROM produk;\n```\n\n## DISTINCT\nMenampilkan nilai unik saja:\n```sql\nSELECT DISTINCT kategori FROM produk;\n```\n\n## Tips\n- Selalu gunakan kolom spesifik daripada `*` di production\n- `SELECT *` bagus untuk eksplorasi, tapi buruk untuk performa",
			ContentMarkdownEN: sp("# Basic SELECT\n\nSELECT is used to retrieve data from a table.\n\n## Selecting All Columns\n```sql\nSELECT * FROM produk;\n```\n\n## Selecting Specific Columns\n```sql\nSELECT nama_produk, harga FROM produk;\n```\n\n## Alias (AS)\n```sql\nSELECT nama_produk AS name, harga AS price FROM produk;\n```\n\n## DISTINCT\nShow unique values only:\n```sql\nSELECT DISTINCT kategori FROM produk;\n```\n\n## Tips\n- Always use specific columns instead of `*` in production\n- `SELECT *` is good for exploration but bad for performance"),
		},
		{ID: lessonUUID(4, 3), CourseID: CourseSQLBeg, OrderIndex: 3, XPReward: 10, TitleID: "WHERE & Filtering", TitleEN: sp("WHERE & Filtering"),
			ContentMarkdownID: "# WHERE & Filtering\n\nWHERE digunakan untuk memfilter baris.\n\n```sql\nSELECT * FROM produk WHERE harga > 100000;\nSELECT * FROM produk WHERE kategori = 'Fashion';\nSELECT * FROM produk WHERE stok >= 10 AND harga < 50000;\nSELECT * FROM produk WHERE kategori = 'Fashion' OR kategori = 'Food';\n```\n\n## Operator yang Bisa Dipakai\n| Operator | Arti |\n|----------|------|\n| = | Sama dengan |\n| != atau <> | Tidak sama |\n| > < >= <= | Perbandingan |\n| AND | Semua kondisi harus benar |\n| OR | Salah satu kondisi benar |\n| NOT | Negasi |",
			ContentMarkdownEN: sp("# WHERE & Filtering\n\nWHERE is used to filter rows.\n\n```sql\nSELECT * FROM produk WHERE harga > 100000;\nSELECT * FROM produk WHERE kategori = 'Fashion';\nSELECT * FROM produk WHERE stok >= 10 AND harga < 50000;\nSELECT * FROM produk WHERE kategori = 'Fashion' OR kategori = 'Food';\n```\n\n## Available Operators\n| Operator | Meaning |\n|----------|------|\n| = | Equal to |\n| != or <> | Not equal |\n| > < >= <= | Comparison |\n| AND | All conditions must be true |\n| OR | Any condition can be true |\n| NOT | Negation |"),
		},
		{ID: lessonUUID(4, 4), CourseID: CourseSQLBeg, OrderIndex: 4, XPReward: 10, TitleID: "ORDER BY, LIMIT & OFFSET", TitleEN: sp("ORDER BY, LIMIT & OFFSET"),
			ContentMarkdownID: "# ORDER BY, LIMIT & OFFSET\n\n## ORDER BY (mengurutkan)\n```sql\nSELECT * FROM produk ORDER BY harga ASC;  -- termurah dulu\nSELECT * FROM produk ORDER BY harga DESC; -- termahal dulu\n```\n\n## LIMIT (membatasi jumlah)\n```sql\nSELECT * FROM produk ORDER BY harga DESC LIMIT 5; -- top 5 termahal\n```\n\n## OFFSET (melewati N baris)\n```sql\nSELECT * FROM produk ORDER BY harga LIMIT 5 OFFSET 10; -- halaman ke-3\n```\n\nKombinasi LIMIT + OFFSET sangat berguna untuk implementasi pagination.",
			ContentMarkdownEN: sp("# ORDER BY, LIMIT & OFFSET\n\n## ORDER BY (sorting)\n```sql\nSELECT * FROM produk ORDER BY harga ASC;  -- cheapest first\nSELECT * FROM produk ORDER BY harga DESC; -- most expensive first\n```\n\n## LIMIT (restrict results)\n```sql\nSELECT * FROM produk ORDER BY harga DESC LIMIT 5; -- top 5 expensive\n```\n\n## OFFSET (skip N rows)\n```sql\nSELECT * FROM produk ORDER BY harga LIMIT 5 OFFSET 10; -- page 3\n```\n\nThe LIMIT + OFFSET combination is very useful for implementing pagination."),
		},
		{ID: lessonUUID(4, 5), CourseID: CourseSQLBeg, OrderIndex: 5, XPReward: 15, TitleID: "INSERT INTO", TitleEN: sp("INSERT INTO"),
			ContentMarkdownID: "# INSERT INTO\n\nMenambahkan data baru ke tabel.\n\n```sql\nINSERT INTO produk (nama_produk, kategori, harga, stok)\nVALUES ('Kopi Luwak', 'Food', 350000, 15);\n```\n\n## Multiple INSERT\n```sql\nINSERT INTO produk (nama_produk, kategori, harga, stok) VALUES\n('Teh Hijau', 'Food', 25000, 100),\n('Sarung Batik', 'Fashion', 120000, 30),\n('Madu Hutan', 'Food', 85000, 50);\n```\n\n## INSERT dengan DEFAULT\n```sql\nINSERT INTO produk (nama_produk, kategori) VALUES ('Test', 'Other');\n-- kolom lain akan menggunakan DEFAULT value\n```",
			ContentMarkdownEN: sp("# INSERT INTO\n\nAdding new data to a table.\n\n```sql\nINSERT INTO produk (nama_produk, kategori, harga, stok)\nVALUES ('Kopi Luwak', 'Food', 350000, 15);\n```\n\n## Multiple INSERT\n```sql\nINSERT INTO produk (nama_produk, kategori, harga, stok) VALUES\n('Green Tea', 'Food', 25000, 100),\n('Batik Sarong', 'Fashion', 120000, 30),\n('Forest Honey', 'Food', 85000, 50);\n```\n\n## INSERT with DEFAULT\n```sql\nINSERT INTO produk (nama_produk, kategori) VALUES ('Test', 'Other');\n-- other columns will use their DEFAULT values\n```"),
		},
		{ID: lessonUUID(4, 6), CourseID: CourseSQLBeg, OrderIndex: 6, XPReward: 15, TitleID: "UPDATE & DELETE", TitleEN: sp("UPDATE & DELETE"),
			ContentMarkdownID: "# UPDATE & DELETE\n\n## UPDATE\n```sql\nUPDATE produk SET harga = 175000 WHERE produk_id = 1;\n\nUPDATE produk SET stok = stok - 1 WHERE nama_produk = 'Batik Tulis';\n```\n\n⚠️ **SELALU gunakan WHERE** — tanpa WHERE semua baris akan berubah!\n\n## DELETE\n```sql\nDELETE FROM produk WHERE produk_id = 5;\n```\n\n⚠️ **SELALU gunakan WHERE** — tanpa WHERE semua data hilang!\n\n## Safe Practice\nLakukan SELECT dulu sebelum UPDATE/DELETE:\n```sql\n-- Cek dulu\nSELECT * FROM produk WHERE stok = 0;\n-- Baru hapus\nDELETE FROM produk WHERE stok = 0;\n```",
			ContentMarkdownEN: sp("# UPDATE & DELETE\n\n## UPDATE\n```sql\nUPDATE produk SET harga = 175000 WHERE produk_id = 1;\n\nUPDATE produk SET stok = stok - 1 WHERE nama_produk = 'Batik Tulis';\n```\n\n⚠️ **ALWAYS use WHERE** — without WHERE all rows will be modified!\n\n## DELETE\n```sql\nDELETE FROM produk WHERE produk_id = 5;\n```\n\n⚠️ **ALWAYS use WHERE** — without WHERE all data will be lost!\n\n## Safe Practice\nAlways SELECT before UPDATE/DELETE:\n```sql\n-- Check first\nSELECT * FROM produk WHERE stok = 0;\n-- Then delete\nDELETE FROM produk WHERE stok = 0;\n```"),
		},
		{ID: lessonUUID(4, 7), CourseID: CourseSQLBeg, OrderIndex: 7, XPReward: 15, TitleID: "Tipe Data SQL", TitleEN: sp("SQL Data Types"),
			ContentMarkdownID: "# Tipe Data SQL\n\n## Tipe Umum PostgreSQL\n| Tipe | Keterangan | Contoh |\n|------|------------|--------|\n| INTEGER | Bilangan bulat | 42 |\n| BIGINT | Integer besar | 9999999999 |\n| NUMERIC(p,s) | Presisi tepat | 99.99 |\n| VARCHAR(n) | String max n karakter | 'hello' |\n| TEXT | String tanpa batas | Paragraf panjang |\n| BOOLEAN | true/false | true |\n| DATE | Tanggal | '2024-01-15' |\n| TIMESTAMP | Tanggal + waktu | '2024-01-15 10:30:00' |\n| UUID | Identifier unik | gen_random_uuid() |\n| JSONB | Data JSON | '{\"key\": \"value\"}' |\n\n## Tips Pemilihan\n- Gunakan `INTEGER` untuk ID auto-increment\n- Gunakan `UUID` untuk distributed systems\n- Gunakan `NUMERIC` untuk uang (bukan FLOAT!)\n- Gunakan `TEXT` jika ukuran string tidak pasti",
			ContentMarkdownEN: sp("# SQL Data Types\n\n## Common PostgreSQL Types\n| Type | Description | Example |\n|------|-------------|--------|\n| INTEGER | Whole numbers | 42 |\n| BIGINT | Large integers | 9999999999 |\n| NUMERIC(p,s) | Exact precision | 99.99 |\n| VARCHAR(n) | String max n chars | 'hello' |\n| TEXT | Unlimited string | Long paragraph |\n| BOOLEAN | true/false | true |\n| DATE | Date | '2024-01-15' |\n| TIMESTAMP | Date + time | '2024-01-15 10:30:00' |\n| UUID | Unique identifier | gen_random_uuid() |\n| JSONB | JSON data | '{\"key\": \"value\"}' |\n\n## Selection Tips\n- Use `INTEGER` for auto-increment IDs\n- Use `UUID` for distributed systems\n- Use `NUMERIC` for money (never FLOAT!)\n- Use `TEXT` if string size is uncertain"),
		},
		{ID: lessonUUID(4, 8), CourseID: CourseSQLBeg, OrderIndex: 8, XPReward: 20, TitleID: "DDL: CREATE, ALTER, DROP", TitleEN: sp("DDL: CREATE, ALTER, DROP"),
			ContentMarkdownID: "# DDL: CREATE, ALTER, DROP\n\n## CREATE TABLE\n```sql\nCREATE TABLE pelanggan (\n    pelanggan_id SERIAL PRIMARY KEY,\n    nama VARCHAR(100) NOT NULL,\n    email VARCHAR(255) UNIQUE,\n    created_at TIMESTAMP DEFAULT NOW()\n);\n```\n\n## ALTER TABLE\n```sql\nALTER TABLE pelanggan ADD COLUMN telepon VARCHAR(20);\nALTER TABLE pelanggan DROP COLUMN telepon;\nALTER TABLE pelanggan RENAME COLUMN nama TO nama_lengkap;\n```\n\n## DROP TABLE\n```sql\nDROP TABLE IF EXISTS pelanggan;\n```\n\n## Constraints\n- `PRIMARY KEY` — identifier unik\n- `NOT NULL` — tidak boleh kosong\n- `UNIQUE` — tidak boleh duplikat\n- `FOREIGN KEY` — referensi ke tabel lain\n- `DEFAULT` — nilai default",
			ContentMarkdownEN: sp("# DDL: CREATE, ALTER, DROP\n\n## CREATE TABLE\n```sql\nCREATE TABLE customers (\n    customer_id SERIAL PRIMARY KEY,\n    name VARCHAR(100) NOT NULL,\n    email VARCHAR(255) UNIQUE,\n    created_at TIMESTAMP DEFAULT NOW()\n);\n```\n\n## ALTER TABLE\n```sql\nALTER TABLE customers ADD COLUMN phone VARCHAR(20);\nALTER TABLE customers DROP COLUMN phone;\nALTER TABLE customers RENAME COLUMN name TO full_name;\n```\n\n## DROP TABLE\n```sql\nDROP TABLE IF EXISTS customers;\n```\n\n## Constraints\n- `PRIMARY KEY` — unique identifier\n- `NOT NULL` — cannot be empty\n- `UNIQUE` — no duplicates allowed\n- `FOREIGN KEY` — reference to another table\n- `DEFAULT` — default value"),
		},
		// ===== sql-intermediate (8 lessons) =====
		{ID: lessonUUID(5, 1), CourseID: CourseSQLMid, OrderIndex: 1, XPReward: 20, TitleID: "Filtering Lanjutan: LIKE, IN, BETWEEN", TitleEN: sp("Advanced Filtering: LIKE, IN, BETWEEN"),
			ContentMarkdownID: "# Filtering Lanjutan\n\n## LIKE (pencarian pattern)\n```sql\nSELECT * FROM produk WHERE nama_produk LIKE 'Batik%';   -- dimulai dengan 'Batik'\nSELECT * FROM produk WHERE nama_produk LIKE '%Kopi%';   -- mengandung 'Kopi'\nSELECT * FROM produk WHERE nama_produk ILIKE '%kopi%';  -- case-insensitive\n```\n\n## IN (daftar nilai)\n```sql\nSELECT * FROM produk WHERE kategori IN ('Fashion', 'Food', 'Craft');\n```\n\n## BETWEEN (rentang)\n```sql\nSELECT * FROM produk WHERE harga BETWEEN 10000 AND 100000;\nSELECT * FROM transaksi WHERE tanggal BETWEEN '2024-01-01' AND '2024-12-31';\n```",
			ContentMarkdownEN: sp("# Advanced Filtering\n\n## LIKE (pattern matching)\n```sql\nSELECT * FROM produk WHERE nama_produk LIKE 'Batik%';   -- starts with 'Batik'\nSELECT * FROM produk WHERE nama_produk LIKE '%Kopi%';   -- contains 'Kopi'\nSELECT * FROM produk WHERE nama_produk ILIKE '%kopi%';  -- case-insensitive\n```\n\n## IN (list of values)\n```sql\nSELECT * FROM produk WHERE kategori IN ('Fashion', 'Food', 'Craft');\n```\n\n## BETWEEN (range)\n```sql\nSELECT * FROM produk WHERE harga BETWEEN 10000 AND 100000;\nSELECT * FROM transaksi WHERE tanggal BETWEEN '2024-01-01' AND '2024-12-31';\n```"),
		},
		{ID: lessonUUID(5, 2), CourseID: CourseSQLMid, OrderIndex: 2, XPReward: 20, TitleID: "IS NULL, COALESCE & CASE WHEN", TitleEN: sp("IS NULL, COALESCE & CASE WHEN"),
			ContentMarkdownID: "# IS NULL, COALESCE & CASE WHEN\n\n## IS NULL\n```sql\nSELECT * FROM produk WHERE deskripsi IS NULL;\nSELECT * FROM produk WHERE deskripsi IS NOT NULL;\n```\n\n## COALESCE (fallback value)\n```sql\nSELECT nama_produk, COALESCE(deskripsi, 'Tidak ada deskripsi') AS deskripsi\nFROM produk;\n```\n\n## CASE WHEN\n```sql\nSELECT nama_produk, harga,\n  CASE\n    WHEN harga > 500000 THEN 'Premium'\n    WHEN harga > 100000 THEN 'Menengah'\n    ELSE 'Murah'\n  END AS kategori_harga\nFROM produk;\n```\n\nCASE WHEN sangat berguna untuk membuat kolom kalkulasi.",
			ContentMarkdownEN: sp("# IS NULL, COALESCE & CASE WHEN\n\n## IS NULL\n```sql\nSELECT * FROM produk WHERE description IS NULL;\nSELECT * FROM produk WHERE description IS NOT NULL;\n```\n\n## COALESCE (fallback value)\n```sql\nSELECT nama_produk, COALESCE(description, 'No description') AS description\nFROM produk;\n```\n\n## CASE WHEN\n```sql\nSELECT nama_produk, harga,\n  CASE\n    WHEN harga > 500000 THEN 'Premium'\n    WHEN harga > 100000 THEN 'Mid-range'\n    ELSE 'Budget'\n  END AS price_category\nFROM produk;\n```\n\nCASE WHEN is very useful for creating calculated columns."),
		},
		{ID: lessonUUID(5, 3), CourseID: CourseSQLMid, OrderIndex: 3, XPReward: 20, TitleID: "Aggregate: COUNT, SUM, AVG, MIN, MAX", TitleEN: sp("Aggregate Functions"),
			ContentMarkdownID: "# Aggregate Functions\n\n```sql\nSELECT\n  COUNT(*) AS jumlah_produk,\n  SUM(stok) AS total_stok,\n  AVG(harga) AS rata_rata_harga,\n  MIN(harga) AS harga_termurah,\n  MAX(harga) AS harga_termahal\nFROM produk;\n```\n\n## COUNT dengan kondisi\n```sql\nSELECT COUNT(*) AS jumlah_fashion\nFROM produk\nWHERE kategori = 'Fashion';\n```\n\nAggregate function mengurangi banyak baris menjadi satu nilai ringkasan.",
			ContentMarkdownEN: sp("# Aggregate Functions\n\n```sql\nSELECT\n  COUNT(*) AS product_count,\n  SUM(stok) AS total_stock,\n  AVG(harga) AS avg_price,\n  MIN(harga) AS min_price,\n  MAX(harga) AS max_price\nFROM produk;\n```\n\n## COUNT with condition\n```sql\nSELECT COUNT(*) AS fashion_count\nFROM produk\nWHERE kategori = 'Fashion';\n```\n\nAggregate functions reduce many rows into a single summary value."),
		},
		{ID: lessonUUID(5, 4), CourseID: CourseSQLMid, OrderIndex: 4, XPReward: 25, TitleID: "GROUP BY & HAVING", TitleEN: sp("GROUP BY & HAVING"),
			ContentMarkdownID: "# GROUP BY & HAVING\n\n## GROUP BY\n```sql\nSELECT kategori, COUNT(*) AS jumlah, AVG(harga) AS rata_rata\nFROM produk\nGROUP BY kategori;\n```\n\n## HAVING (filter setelah grouping)\n```sql\nSELECT kategori, COUNT(*) AS jumlah\nFROM produk\nGROUP BY kategori\nHAVING COUNT(*) > 3;\n```\n\n### WHERE vs HAVING\n- WHERE: filter baris **sebelum** grouping\n- HAVING: filter group **setelah** grouping\n\n```sql\nSELECT kategori, AVG(harga) AS avg\nFROM produk\nWHERE stok > 0          -- filter dulu\nGROUP BY kategori\nHAVING AVG(harga) > 50000; -- filter group\n```",
			ContentMarkdownEN: sp("# GROUP BY & HAVING\n\n## GROUP BY\n```sql\nSELECT kategori, COUNT(*) AS count, AVG(harga) AS avg_price\nFROM produk\nGROUP BY kategori;\n```\n\n## HAVING (filter after grouping)\n```sql\nSELECT kategori, COUNT(*) AS count\nFROM produk\nGROUP BY kategori\nHAVING COUNT(*) > 3;\n```\n\n### WHERE vs HAVING\n- WHERE: filters rows **before** grouping\n- HAVING: filters groups **after** grouping\n\n```sql\nSELECT kategori, AVG(harga) AS avg\nFROM produk\nWHERE stok > 0          -- filter first\nGROUP BY kategori\nHAVING AVG(harga) > 50000; -- filter groups\n```"),
		},
		{ID: lessonUUID(5, 5), CourseID: CourseSQLMid, OrderIndex: 5, XPReward: 25, TitleID: "INNER JOIN & Konsepnya", TitleEN: sp("INNER JOIN & Concepts"),
			ContentMarkdownID: "# INNER JOIN\n\nJOIN menggabungkan data dari dua atau lebih tabel.\n\n```sql\nSELECT t.transaksi_id, p.nama_pelanggan, pr.nama_produk, t.jumlah\nFROM transaksi t\nINNER JOIN pelanggan p ON t.pelanggan_id = p.pelanggan_id\nINNER JOIN produk pr ON t.produk_id = pr.produk_id;\n```\n\nINNER JOIN hanya mengembalikan baris yang ada kecocokan di KEDUA tabel.\n\n## Alias Tabel\nGunakan alias (t, p, pr) agar query lebih ringkas dan mudah dibaca.",
			ContentMarkdownEN: sp("# INNER JOIN\n\nJOIN combines data from two or more tables.\n\n```sql\nSELECT t.transaksi_id, p.nama_pelanggan, pr.nama_produk, t.jumlah\nFROM transaksi t\nINNER JOIN pelanggan p ON t.pelanggan_id = p.pelanggan_id\nINNER JOIN produk pr ON t.produk_id = pr.produk_id;\n```\n\nINNER JOIN only returns rows where there's a match in BOTH tables.\n\n## Table Aliases\nUse aliases (t, p, pr) to make queries more concise and readable."),
		},
		{ID: lessonUUID(5, 6), CourseID: CourseSQLMid, OrderIndex: 6, XPReward: 25, TitleID: "LEFT, RIGHT & FULL JOIN", TitleEN: sp("LEFT, RIGHT & FULL JOIN"),
			ContentMarkdownID: "# LEFT, RIGHT & FULL JOIN\n\n## LEFT JOIN\nSemua baris dari tabel kiri, dengan data dari kanan jika ada:\n```sql\nSELECT pr.nama_produk, t.transaksi_id\nFROM produk pr\nLEFT JOIN transaksi t ON pr.produk_id = t.produk_id;\n```\nProduk tanpa transaksi tetap muncul (dengan NULL untuk kolom transaksi).\n\n## RIGHT JOIN\nKebalikan dari LEFT JOIN.\n\n## FULL OUTER JOIN\nSemua baris dari kedua tabel:\n```sql\nSELECT * FROM produk\nFULL OUTER JOIN transaksi ON produk.produk_id = transaksi.produk_id;\n```",
			ContentMarkdownEN: sp("# LEFT, RIGHT & FULL JOIN\n\n## LEFT JOIN\nAll rows from the left table, with data from the right if it exists:\n```sql\nSELECT pr.nama_produk, t.transaksi_id\nFROM produk pr\nLEFT JOIN transaksi t ON pr.produk_id = t.produk_id;\n```\nProducts without transactions still appear (with NULL for transaction columns).\n\n## RIGHT JOIN\nThe opposite of LEFT JOIN.\n\n## FULL OUTER JOIN\nAll rows from both tables:\n```sql\nSELECT * FROM produk\nFULL OUTER JOIN transaksi ON produk.produk_id = transaksi.produk_id;\n```"),
		},
		{ID: lessonUUID(5, 7), CourseID: CourseSQLMid, OrderIndex: 7, XPReward: 30, TitleID: "Subquery", TitleEN: sp("Subqueries"),
			ContentMarkdownID: "# Subquery\n\nSubquery adalah query di dalam query lain.\n\n## Di WHERE\n```sql\nSELECT * FROM produk\nWHERE harga > (SELECT AVG(harga) FROM produk);\n```\n\n## Di FROM\n```sql\nSELECT kategori, avg_price\nFROM (\n  SELECT kategori, AVG(harga) AS avg_price\n  FROM produk GROUP BY kategori\n) AS sub\nWHERE avg_price > 100000;\n```\n\n## Di SELECT\n```sql\nSELECT nama_produk, harga,\n  (SELECT AVG(harga) FROM produk) AS rata_rata_global\nFROM produk;\n```",
			ContentMarkdownEN: sp("# Subqueries\n\nA subquery is a query inside another query.\n\n## In WHERE\n```sql\nSELECT * FROM produk\nWHERE harga > (SELECT AVG(harga) FROM produk);\n```\n\n## In FROM\n```sql\nSELECT kategori, avg_price\nFROM (\n  SELECT kategori, AVG(harga) AS avg_price\n  FROM produk GROUP BY kategori\n) AS sub\nWHERE avg_price > 100000;\n```\n\n## In SELECT\n```sql\nSELECT nama_produk, harga,\n  (SELECT AVG(harga) FROM produk) AS global_average\nFROM produk;\n```"),
		},
		{ID: lessonUUID(5, 8), CourseID: CourseSQLMid, OrderIndex: 8, XPReward: 25, TitleID: "Fungsi String & Tanggal", TitleEN: sp("String & Date Functions"),
			ContentMarkdownID: "# Fungsi String & Tanggal\n\n## String Functions\n```sql\nSELECT\n  UPPER('hello'),            -- 'HELLO'\n  LOWER('HELLO'),            -- 'hello'\n  LENGTH('NgodingYuk'),      -- 10\n  CONCAT('Hello', ' World'), -- 'Hello World'\n  SUBSTRING('abcdef', 2, 3), -- 'bcd'\n  TRIM('  hello  ');         -- 'hello'\n```\n\n## Date Functions\n```sql\nSELECT\n  NOW(),                                -- current timestamp\n  CURRENT_DATE,                         -- today's date\n  DATE_TRUNC('month', NOW()),           -- first day of month\n  EXTRACT(YEAR FROM NOW()),             -- current year\n  AGE(NOW(), '1995-06-15'::timestamp);  -- age calculation\n```",
			ContentMarkdownEN: sp("# String & Date Functions\n\n## String Functions\n```sql\nSELECT\n  UPPER('hello'),            -- 'HELLO'\n  LOWER('HELLO'),            -- 'hello'\n  LENGTH('NgodingYuk'),      -- 10\n  CONCAT('Hello', ' World'), -- 'Hello World'\n  SUBSTRING('abcdef', 2, 3), -- 'bcd'\n  TRIM('  hello  ');         -- 'hello'\n```\n\n## Date Functions\n```sql\nSELECT\n  NOW(),                                -- current timestamp\n  CURRENT_DATE,                         -- today's date\n  DATE_TRUNC('month', NOW()),           -- first day of month\n  EXTRACT(YEAR FROM NOW()),             -- current year\n  AGE(NOW(), '1995-06-15'::timestamp);  -- age calculation\n```"),
		},
		// ===== sql-advanced (8 lessons) =====
		{ID: lessonUUID(6, 1), CourseID: CourseSQLAdv, OrderIndex: 1, XPReward: 35, TitleID: "Window Functions: Pengenalan", TitleEN: sp("Window Functions: Introduction"),
			ContentMarkdownID: "# Window Functions\n\nWindow function melakukan kalkulasi pada set baris yang berhubungan dengan baris saat ini — tanpa mengurangi jumlah baris seperti GROUP BY.\n\n```sql\nSELECT nama_produk, kategori, harga,\n  AVG(harga) OVER (PARTITION BY kategori) AS avg_kategori\nFROM produk;\n```\n\n## Sintaks\n```\nFUNCTION() OVER (\n  PARTITION BY kolom    -- opsional: grouping\n  ORDER BY kolom        -- opsional: urutan\n)\n```\n\n## Perbedaan dengan GROUP BY\nGROUP BY: 1 baris per group\nWindow: semua baris tetap ada, plus kolom kalkulasi",
			ContentMarkdownEN: sp("# Window Functions\n\nWindow functions perform calculations across a set of rows related to the current row — without reducing the number of rows like GROUP BY.\n\n```sql\nSELECT nama_produk, kategori, harga,\n  AVG(harga) OVER (PARTITION BY kategori) AS category_avg\nFROM produk;\n```\n\n## Syntax\n```\nFUNCTION() OVER (\n  PARTITION BY column    -- optional: grouping\n  ORDER BY column        -- optional: ordering\n)\n```\n\n## Difference from GROUP BY\nGROUP BY: 1 row per group\nWindow: all rows remain, plus calculated column"),
		},
		{ID: lessonUUID(6, 2), CourseID: CourseSQLAdv, OrderIndex: 2, XPReward: 35, TitleID: "ROW_NUMBER, RANK & DENSE_RANK", TitleEN: sp("ROW_NUMBER, RANK & DENSE_RANK"),
			ContentMarkdownID: "# ROW_NUMBER, RANK & DENSE_RANK\n\n```sql\nSELECT nama_produk, harga,\n  ROW_NUMBER() OVER (ORDER BY harga DESC) AS rn,\n  RANK()       OVER (ORDER BY harga DESC) AS rnk,\n  DENSE_RANK() OVER (ORDER BY harga DESC) AS drnk\nFROM produk;\n```\n\n## Perbedaan\n- **ROW_NUMBER**: 1, 2, 3, 4 (selalu unik)\n- **RANK**: 1, 2, 2, 4 (loncat jika sama)\n- **DENSE_RANK**: 1, 2, 2, 3 (tidak loncat)\n\n## Top-N per Group\n```sql\nSELECT * FROM (\n  SELECT *, ROW_NUMBER() OVER (PARTITION BY kategori ORDER BY harga DESC) AS rn\n  FROM produk\n) sub WHERE rn <= 3;\n```",
			ContentMarkdownEN: sp("# ROW_NUMBER, RANK & DENSE_RANK\n\n```sql\nSELECT nama_produk, harga,\n  ROW_NUMBER() OVER (ORDER BY harga DESC) AS rn,\n  RANK()       OVER (ORDER BY harga DESC) AS rnk,\n  DENSE_RANK() OVER (ORDER BY harga DESC) AS drnk\nFROM produk;\n```\n\n## Differences\n- **ROW_NUMBER**: 1, 2, 3, 4 (always unique)\n- **RANK**: 1, 2, 2, 4 (skips on ties)\n- **DENSE_RANK**: 1, 2, 2, 3 (no gaps)\n\n## Top-N per Group\n```sql\nSELECT * FROM (\n  SELECT *, ROW_NUMBER() OVER (PARTITION BY kategori ORDER BY harga DESC) AS rn\n  FROM produk\n) sub WHERE rn <= 3;\n```"),
		},
		{ID: lessonUUID(6, 3), CourseID: CourseSQLAdv, OrderIndex: 3, XPReward: 35, TitleID: "LAG, LEAD & Running Total", TitleEN: sp("LAG, LEAD & Running Total"),
			ContentMarkdownID: "# LAG, LEAD & Running Total\n\n## LAG (nilai baris sebelumnya)\n```sql\nSELECT tanggal, total,\n  LAG(total) OVER (ORDER BY tanggal) AS prev_total\nFROM penjualan;\n```\n\n## LEAD (nilai baris berikutnya)\n```sql\nSELECT tanggal, total,\n  LEAD(total) OVER (ORDER BY tanggal) AS next_total\nFROM penjualan;\n```\n\n## Running Total\n```sql\nSELECT tanggal, total,\n  SUM(total) OVER (ORDER BY tanggal) AS running_total\nFROM penjualan;\n```",
			ContentMarkdownEN: sp("# LAG, LEAD & Running Total\n\n## LAG (previous row value)\n```sql\nSELECT tanggal, total,\n  LAG(total) OVER (ORDER BY tanggal) AS prev_total\nFROM sales;\n```\n\n## LEAD (next row value)\n```sql\nSELECT tanggal, total,\n  LEAD(total) OVER (ORDER BY tanggal) AS next_total\nFROM sales;\n```\n\n## Running Total\n```sql\nSELECT tanggal, total,\n  SUM(total) OVER (ORDER BY tanggal) AS running_total\nFROM sales;\n```"),
		},
		{ID: lessonUUID(6, 4), CourseID: CourseSQLAdv, OrderIndex: 4, XPReward: 35, TitleID: "CTE dengan WITH", TitleEN: sp("CTE with WITH"),
			ContentMarkdownID: "# CTE (Common Table Expression)\n\nCTE membuat query lebih mudah dibaca dengan memecahnya menjadi bagian-bagian bernama.\n\n```sql\nWITH top_products AS (\n  SELECT produk_id, SUM(jumlah) AS total_sold\n  FROM transaksi GROUP BY produk_id\n)\nSELECT p.nama_produk, tp.total_sold\nFROM produk p\nJOIN top_products tp ON p.produk_id = tp.produk_id\nORDER BY tp.total_sold DESC;\n```\n\n## Multiple CTEs\n```sql\nWITH\ncte1 AS (SELECT ...),\ncte2 AS (SELECT ... FROM cte1)\nSELECT * FROM cte2;\n```",
			ContentMarkdownEN: sp("# CTE (Common Table Expression)\n\nCTE makes queries more readable by breaking them into named parts.\n\n```sql\nWITH top_products AS (\n  SELECT produk_id, SUM(jumlah) AS total_sold\n  FROM transaksi GROUP BY produk_id\n)\nSELECT p.nama_produk, tp.total_sold\nFROM produk p\nJOIN top_products tp ON p.produk_id = tp.produk_id\nORDER BY tp.total_sold DESC;\n```\n\n## Multiple CTEs\n```sql\nWITH\ncte1 AS (SELECT ...),\ncte2 AS (SELECT ... FROM cte1)\nSELECT * FROM cte2;\n```"),
		},
		{ID: lessonUUID(6, 5), CourseID: CourseSQLAdv, OrderIndex: 5, XPReward: 40, TitleID: "Recursive CTE", TitleEN: sp("Recursive CTE"),
			ContentMarkdownID: "# Recursive CTE\n\nRecursive CTE bisa mereferensi dirinya sendiri — berguna untuk data hirarkis.\n\n```sql\nWITH RECURSIVE subordinates AS (\n  SELECT id, name, manager_id, 1 AS level\n  FROM employees WHERE manager_id IS NULL\n  UNION ALL\n  SELECT e.id, e.name, e.manager_id, s.level + 1\n  FROM employees e\n  JOIN subordinates s ON e.manager_id = s.id\n)\nSELECT * FROM subordinates ORDER BY level;\n```\n\nRecursive CTE selalu memiliki:\n1. **Base case** (non-recursive)\n2. **UNION ALL**\n3. **Recursive term** (mereferensi CTE sendiri)",
			ContentMarkdownEN: sp("# Recursive CTE\n\nRecursive CTE can reference itself — useful for hierarchical data.\n\n```sql\nWITH RECURSIVE subordinates AS (\n  SELECT id, name, manager_id, 1 AS level\n  FROM employees WHERE manager_id IS NULL\n  UNION ALL\n  SELECT e.id, e.name, e.manager_id, s.level + 1\n  FROM employees e\n  JOIN subordinates s ON e.manager_id = s.id\n)\nSELECT * FROM subordinates ORDER BY level;\n```\n\nRecursive CTEs always have:\n1. **Base case** (non-recursive)\n2. **UNION ALL**\n3. **Recursive term** (references the CTE itself)"),
		},
		{ID: lessonUUID(6, 6), CourseID: CourseSQLAdv, OrderIndex: 6, XPReward: 40, TitleID: "Index & EXPLAIN ANALYZE", TitleEN: sp("Index & EXPLAIN ANALYZE"),
			ContentMarkdownID: "# Index & EXPLAIN ANALYZE\n\n## Index\nIndex mempercepat pencarian data:\n```sql\nCREATE INDEX idx_produk_kategori ON produk(kategori);\nCREATE INDEX idx_transaksi_tanggal ON transaksi(tanggal);\n```\n\n## EXPLAIN ANALYZE\nMelihat query plan:\n```sql\nEXPLAIN ANALYZE SELECT * FROM produk WHERE kategori = 'Fashion';\n```\n\nPerhatikan: Seq Scan (lambat) vs Index Scan (cepat)\n\n## Kapan Pakai Index?\n- Kolom yang sering di-WHERE\n- Kolom yang sering di-JOIN\n- Kolom yang sering di-ORDER BY",
			ContentMarkdownEN: sp("# Index & EXPLAIN ANALYZE\n\n## Index\nIndexes speed up data lookup:\n```sql\nCREATE INDEX idx_produk_kategori ON produk(kategori);\nCREATE INDEX idx_transaksi_tanggal ON transaksi(tanggal);\n```\n\n## EXPLAIN ANALYZE\nView query plan:\n```sql\nEXPLAIN ANALYZE SELECT * FROM produk WHERE kategori = 'Fashion';\n```\n\nLook for: Seq Scan (slow) vs Index Scan (fast)\n\n## When to Use Index?\n- Columns frequently in WHERE\n- Columns frequently in JOIN\n- Columns frequently in ORDER BY"),
		},
		{ID: lessonUUID(6, 7), CourseID: CourseSQLAdv, OrderIndex: 7, XPReward: 40, TitleID: "Transaksi & ACID", TitleEN: sp("Transactions & ACID"),
			ContentMarkdownID: "# Transaksi & ACID\n\n## Transaction\n```sql\nBEGIN;\nUPDATE accounts SET balance = balance - 100 WHERE id = 1;\nUPDATE accounts SET balance = balance + 100 WHERE id = 2;\nCOMMIT;\n```\n\nJika ada error:\n```sql\nROLLBACK;\n```\n\n## ACID Properties\n- **Atomicity**: semua atau tidak sama sekali\n- **Consistency**: data selalu valid\n- **Isolation**: transaksi tidak saling mengganggu\n- **Durability**: data tersimpan permanen setelah commit",
			ContentMarkdownEN: sp("# Transactions & ACID\n\n## Transaction\n```sql\nBEGIN;\nUPDATE accounts SET balance = balance - 100 WHERE id = 1;\nUPDATE accounts SET balance = balance + 100 WHERE id = 2;\nCOMMIT;\n```\n\nIf there's an error:\n```sql\nROLLBACK;\n```\n\n## ACID Properties\n- **Atomicity**: all or nothing\n- **Consistency**: data always valid\n- **Isolation**: transactions don't interfere\n- **Durability**: data persists after commit"),
		},
		{ID: lessonUUID(6, 8), CourseID: CourseSQLAdv, OrderIndex: 8, XPReward: 35, TitleID: "Desain Database & Normalisasi", TitleEN: sp("Database Design & Normalization"),
			ContentMarkdownID: "# Desain Database & Normalisasi\n\n## Normalisasi\nProses mengorganisir data untuk mengurangi redundansi.\n\n### 1NF: Setiap kolom harus atomic\n❌ hobi: 'membaca, coding'\n✅ Buat tabel terpisah untuk hobi\n\n### 2NF: Semua non-key kolom bergantung pada primary key\n### 3NF: Tidak ada transitive dependency\n\n## Relationship Types\n- **One-to-Many**: User → Orders\n- **Many-to-Many**: Students ↔ Courses (perlu junction table)\n- **One-to-One**: User → Profile\n\n## Tips Praktis\n1. Mulai dari 3NF, denormalisasi jika perlu untuk performa\n2. Gunakan foreign key constraints\n3. Selalu pikirkan indexing dari awal",
			ContentMarkdownEN: sp("# Database Design & Normalization\n\n## Normalization\nThe process of organizing data to reduce redundancy.\n\n### 1NF: Each column must be atomic\n❌ hobbies: 'reading, coding'\n✅ Create a separate table for hobbies\n\n### 2NF: All non-key columns depend on the primary key\n### 3NF: No transitive dependencies\n\n## Relationship Types\n- **One-to-Many**: User → Orders\n- **Many-to-Many**: Students ↔ Courses (needs junction table)\n- **One-to-One**: User → Profile\n\n## Practical Tips\n1. Start with 3NF, denormalize for performance if needed\n2. Use foreign key constraints\n3. Always think about indexing from the start"),
		},
	}

	for i := range lessons {
		upsertLesson(db, &lessons[i])
	}
}
